package hellfirepvp.observerlib.api;

import net.minecraft.core.BlockPos;
import net.minecraft.world.level().LevelAccessor;

/**
 * Stub class for ObserverHelper from observerlib.
 */
public class ObserverHelper {
    
    public static void registerObserver(LevelAccessor world, BlockPos pos, ChangeSubscriber subscriber) {
        // Stub implementation
    }
    
    public static void unregisterObserver(LevelAccessor world, BlockPos pos, ChangeSubscriber subscriber) {
        // Stub implementation
    }
}
